language: c
os: linux
sudo: required

compiler:
    - gcc

services:
    - docker

script:
    - docker pull ubuntu:16.04
    - docker run -i -t -v $PWD:/lvg ubuntu:16.04 /lvg/scripts/docker-build.sh
