dependencies:
    pre:
        - sudo apt-get install build-essential git mercurial cmake yasm libsdl2-dev libavcodec-dev mingw-w64 zip wget
compile:
  override:
    - bash -c ./build.sh
    - bash -c ./build_win.sh

test:
  override:
    - true
